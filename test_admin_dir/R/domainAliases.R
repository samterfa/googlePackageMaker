
	#' Deletes a domain Alias of the customer.
	#'
	#' Autogenerated via \code{\link[googlePackageMaker]{generate_google_package}}
	#'
	#' @seealso \href{http://developers.google.com/admin-sdk/}{Google Documentation}
	#'
	#' @details
	#' Authentication scopes used by this function are:
	#' \itemize{
	#' \item https://www.googleapis.com/auth/admin.directory.domain
	#' }
	#' @param customer Immutable ID of the G Suite account.
	#' @param domainAliasName Name of domain alias to be retrieved.
	#' @param fields Selector specifying which fields to include in a partial response.
	#' @param token A token prepared by one of gargle's token generating functions. Defaults to gargle::token_fetch(scopes = \code{\link[googlePackageMaker]{get_function_scopes}}(function_name). See \code{\link[gargle]{token_fetch}} for more info.
	#' @param return_response Whether to return the response or the response content. Defaults to FALSE (return response content).
	#' @export
	directory.domainAliases.delete <- function(customer = NULL, domainAliasName = NULL, token = gargle::token_fetch(scopes = get_function_scopes('directory.domainAliases.delete')), return_response = F, fields = NULL){
		params <- as.list(environment())[!names(as.list(environment())) %in% c('body', 'return_response', 'token')]
		req <- gargle::request_build(method = 'DELETE', path = 'admin/directory/v1/customer/{customer}/domainaliases/{domainAliasName}', params = params, body = NULL, token = token, base_url = 'https://admin.googleapis.com/')
		res <- gargle::request_make(req, encode = 'json')
		if(return_response) return(res)
		httr::content(res)
	}

	#' Retrieves a domain alias of the customer.
	#'
	#' Autogenerated via \code{\link[googlePackageMaker]{generate_google_package}}
	#'
	#' @seealso \href{http://developers.google.com/admin-sdk/}{Google Documentation}
	#'
	#' @details
	#' Authentication scopes used by this function are:
	#' \itemize{
	#' \item https://www.googleapis.com/auth/admin.directory.domain
	#' \item https://www.googleapis.com/auth/admin.directory.domain.readonly
	#' }
	#' @param customer Immutable ID of the G Suite account.
	#' @param domainAliasName Name of domain alias to be retrieved.
	#' @param fields Selector specifying which fields to include in a partial response. Possible fields are domainAliasName, etag, parentDomainName, creationTime, kind, verified.
	#' @param token A token prepared by one of gargle's token generating functions. Defaults to gargle::token_fetch(scopes = \code{\link[googlePackageMaker]{get_function_scopes}}(function_name). See \code{\link[gargle]{token_fetch}} for more info.
	#' @param return_response Whether to return the response or the response content. Defaults to FALSE (return response content).
	#' @export
	directory.domainAliases.get <- function(customer = NULL, domainAliasName = NULL, token = gargle::token_fetch(scopes = get_function_scopes('directory.domainAliases.get')), return_response = F, fields = NULL){
		params <- as.list(environment())[!names(as.list(environment())) %in% c('body', 'return_response', 'token')]
		req <- gargle::request_build(method = 'GET', path = 'admin/directory/v1/customer/{customer}/domainaliases/{domainAliasName}', params = params, body = NULL, token = token, base_url = 'https://admin.googleapis.com/')
		res <- gargle::request_make(req, encode = 'json')
		if(return_response) return(res)
		httr::content(res)
	}

	#' Inserts a domain alias of the customer.
	#'
	#' Autogenerated via \code{\link[googlePackageMaker]{generate_google_package}}
	#'
	#' @seealso \href{http://developers.google.com/admin-sdk/}{Google Documentation}
	#'
	#' @details
	#' Authentication scopes used by this function are:
	#' \itemize{
	#' \item https://www.googleapis.com/auth/admin.directory.domain
	#' }
	#' @param customer Immutable ID of the G Suite account.
	#' @param fields Selector specifying which fields to include in a partial response. Possible fields are .
	#' @param token A token prepared by one of gargle's token generating functions. Defaults to gargle::token_fetch(scopes = \code{\link[googlePackageMaker]{get_function_scopes}}(function_name). See \code{\link[gargle]{token_fetch}} for more info.
	#' @param return_response Whether to return the response or the response content. Defaults to FALSE (return response content).
	#' @export
	directory.domainAliases.insert <- function(customer = NULL, token = gargle::token_fetch(scopes = get_function_scopes('directory.domainAliases.insert')), return_response = F, fields = NULL){
		params <- as.list(environment())[!names(as.list(environment())) %in% c('body', 'return_response', 'token')]
		req <- gargle::request_build(method = 'POST', path = 'admin/directory/v1/customer/{customer}/domainaliases', params = params, body = NULL, token = token, base_url = 'https://admin.googleapis.com/')
		res <- gargle::request_make(req, encode = 'json')
		if(return_response) return(res)
		httr::content(res)
	}

	#' Lists the domain aliases of the customer.
	#'
	#' Autogenerated via \code{\link[googlePackageMaker]{generate_google_package}}
	#'
	#' @seealso \href{http://developers.google.com/admin-sdk/}{Google Documentation}
	#'
	#' @details
	#' Authentication scopes used by this function are:
	#' \itemize{
	#' \item https://www.googleapis.com/auth/admin.directory.domain
	#' \item https://www.googleapis.com/auth/admin.directory.domain.readonly
	#' }
	#' @param customer Immutable ID of the G Suite account.
	#' @param fields Selector specifying which fields to include in a partial response. Possible fields are etag, kind, domainAliases.
	#' @param token A token prepared by one of gargle's token generating functions. Defaults to gargle::token_fetch(scopes = \code{\link[googlePackageMaker]{get_function_scopes}}(function_name). See \code{\link[gargle]{token_fetch}} for more info.
	#' @param return_response Whether to return the response or the response content. Defaults to FALSE (return response content).
	#' @export
	directory.domainAliases.list <- function(customer = NULL, token = gargle::token_fetch(scopes = get_function_scopes('directory.domainAliases.list')), return_response = F, fields = NULL){
		params <- as.list(environment())[!names(as.list(environment())) %in% c('body', 'return_response', 'token')]
		req <- gargle::request_build(method = 'GET', path = 'admin/directory/v1/customer/{customer}/domainaliases', params = params, body = NULL, token = token, base_url = 'https://admin.googleapis.com/')
		res <- gargle::request_make(req, encode = 'json')
		if(return_response) return(res)
		httr::content(res)
	}
